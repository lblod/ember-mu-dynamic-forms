{{#unless prepopulateSolution.isRunning}}
  {{component childComponentName model=model solution=solution options=model.options register=(action "registerChildComponent") value=value updateValue=(action "updateValue")}}
{{/unless}}

{{#if (is-pending model.dynamicSubforms)}}
  Aan het laden...
{{/if}}

{{#if (await subform)}}
  {{form-node model=(get (await subform) 'formNode') solution=solution show=show register=(action "registerSubForm")}}
{{/if}}


<hr />
Edit-child information:

<br />
Value: {{value}}

<br />
Union States of subform:
{{#each subForm.unionStates as |key|}}
  {{anyKey}}
{{/each}}

<br />
Union States of child component:
{{#each childComponent.unionStates as |key|}}
  {{key}}
{{/each}}

<br />
Union States:
{{#each unionStates as |key|}}
  {{key}}
{{/each}}

<br />
Intersection States:
{{#each intersectionStates as |key|}}
  {{key}}
{{/each}}


<hr />
