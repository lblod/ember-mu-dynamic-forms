{{#unless prepopulateSolution.isRunning}}
  {{component childComponentName model=model solution=solution options=model.options}}
{{/unless}}

{{#if (is-pending model.dynamicSubforms)}}
  Aan het laden...
{{/if}}

{{#if (await subform)}}
  {{form-node model=(get (await subform) 'formNode') solution=solution show=show value=value updateValue=(action "updateValue")}}
{{/if}}
