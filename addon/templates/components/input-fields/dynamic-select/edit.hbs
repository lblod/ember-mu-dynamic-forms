<div class="power-select">
  {{#power-select
     allowClear=allowClear
     disabled=disabled
     options=initOptions
     search=(perform search)
     selected=internalValue
     onchange=(action "select")
     as |internalValue|}}
    {{get internalValue displayProperty}}
  {{/power-select}}
</div>
