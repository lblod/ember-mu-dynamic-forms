{{component childComponentName model=model solution=solution options=model.options value=value}}

{{#if subform.isRunning}}
  loading...
{{/if}}

{{#if (await subform)}}
  {{form-node model=(get (await subform) 'formNode') solution=solution show=show}}
{{/if}}
